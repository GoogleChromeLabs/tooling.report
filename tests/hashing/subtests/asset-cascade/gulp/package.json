{
  "private": true,
  "scripts": {
    "build": "NUM=1 npm run -s build:one && NUM=2 npm run -s build:one && NUM=3 npm run -s build:one",
    "build:one": "cp -rf src$NUM src && gulp && rm -rf src"
  },
  "devDependencies": {
    "browserify": "^16.5.0",
    "gulp": "^4.0.2",
    "gulp-buffer": "0.0.2",
    "gulp-rev-all": "^2.0.2",
    "gulp-tap": "^2.0.0",
    "urify-emitter": "^1.1.2"
  }
}
