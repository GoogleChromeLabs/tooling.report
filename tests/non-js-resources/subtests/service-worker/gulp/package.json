{
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "rm -rf build && gulp"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-rev-all": "^2.0.2"
  },
  "dependencies": {
    "browserify": "^16.5.1",
    "gulp-buffer": "0.0.2",
    "gulp-tap": "^2.0.0"
  }
}
